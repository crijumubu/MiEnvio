import 'package:flutter/material.dart';

class NavigatorShippings extends StatelessWidget {
  const NavigatorShippings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Envios"),
    );
  }
}
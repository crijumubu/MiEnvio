import 'package:flutter/material.dart';

class NavigatorSettings extends StatelessWidget {
  const NavigatorSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Settings"),
    );
  }
}